spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgres?currentSchema=simple_home_work
    password: postgres
    username: postgres
server:
  port: 8081